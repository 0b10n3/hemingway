# Gráficos — O Mundo Invertido das Carreiras

Nenhum gráfico neste post.

`graf-01` (duas trilhas verticais com marcadores numerados, comparando a ordem de referência
de construção de conhecimento em finanças com a ordem que o autor de fato seguiu) foi
substituído a pedido do autor no gate humano por uma ilustração gerada via Nano Banana Pro —
ver `ilu-02` em `ilustracoes.md`, que carrega a mesma ideia (duas trilhas, uma contínua e uma
interrompida) em formato de imagem em vez de gráfico de dados.

Código, CSV e figuras do `graf-01` removidos de `graficos/` e `figuras/` nesta revisão — a
decisão e o motivo ficam registrados em `estado.json` (`reentradas`) e no histórico do git
(commit da revisão de visuais que fez essa troca), não é preciso manter o código morto no
diretório.
